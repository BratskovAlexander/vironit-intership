// import service from "../service/service";

// const getCities = async () => {
//   const changeCity = await service.getCity();
//   return changeCity[0];
// };

export const initialState = {
  city: "Minsk"
};

export const citiesReducer = (state: any = initialState) => {
  return state;
};
